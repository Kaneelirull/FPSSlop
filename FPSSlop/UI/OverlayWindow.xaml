<Window x:Class="FPSSlop.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FPSSlop"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Left="10" Top="10"
        SizeToContent="WidthAndHeight">

    <Border x:Name="RootBorder"
            Background="#CC0D0D0D"
            BorderBrush="#2A2A2A"
            BorderThickness="1"
            Padding="10,8">

        <!-- Rows injected in code-behind so order is configurable -->
        <StackPanel x:Name="MetricsPanel" Orientation="Vertical"/>

    </Border>
</Window>
